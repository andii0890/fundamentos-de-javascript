//SUMA
var edad = 27

edad += 1 // esto es igual a: edad = edad + 1

//RESTA
var peso = 75

peso -= 2 // esto es igual a peso = peso - 2

//OPERACIONES

var sandwich = 1

peso += sandwich //peso = peso + sandwich

var jugarAlFutbol = 3

peso -= jugarAlFutbol //peso = peso - jugarAlFutbol

//DECIMALES
var precioDeVino = 200.3

var total = Math.round(precioDeVino * 100 * 3 ) / 100 // Math.round se usa para redondear una operación

var totalStr = total.toFixed(2) // Muestra la cantidad de decimales que se quiera mostrar (se muestra en string)

var total2 = parseFloat(totalStr) // convierte string en número

//DIVISIÓN
var pizza = 8
var personas = 2
var cantidadDePorcionesPorPersona =pizza / personas


